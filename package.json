{
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.0.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.4",
    "cypress": "^14.3.2",
    "cypress-mochawesome-reporter": "^3.8.2",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "scripts": {
    "test:chrome": "npx cypress run --browser chrome --headed",
    "test:edge": "npx cypress run --browser edge --headed",
    "test:electron": "npx cypress run --browser electron --headed",
     "test:firefox": "npx cypress run --browser firefox --headed",
     "test:parallel": "npm run test:chrome && npm run test:edge && npm run test:electron && npm run test:firefox"
  }
}
